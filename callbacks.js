a = {id: 1, name: "John", period: "3ro"};
b = {id: 2, name: "Pete", period: "4to"};
c = {id: 3, name: "Mary", period: "5to"};

users = [a, b, c];

users.forEach(user => {
	console.log(user.period)});

users.forEach(callback(item, index, users) => {array});
